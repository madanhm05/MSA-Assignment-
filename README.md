Server Setup

Navigate to the project folder: cd server

Run the command: npm install

Run the command: node app.js

Open a new terminal.

UI Setup

Navigate to the project folder.

Run the command: npm install

Run the command: npm run dev

The application runs at: http://localhost:5173

Login Credentials

Username: Admin
Password: admin